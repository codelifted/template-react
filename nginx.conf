server {
    listen 80;
    server_name 52.166.110.168;

    location /frontend {
        alias /usr/share/nginx/html;
        try_files $uri $uri/ /frontend/index.html;
    }

    location /frontend/static/ {
        alias /usr/share/nginx/html/static/;
    }
}